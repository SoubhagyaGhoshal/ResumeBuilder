{
  "buildCommand": "pnpm -w run build:pkg && pnpm --filter ResumeBuilder run build",
  "outputDirectory": "site/.output/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nuxtjs"
} 