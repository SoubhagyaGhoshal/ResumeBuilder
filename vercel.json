{
  "buildCommand": "pnpm -w run build:pkg && cd site && pnpm run build",
  "outputDirectory": "site/.output/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nuxtjs"
} 